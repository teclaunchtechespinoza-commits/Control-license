From 2f1a4c0 Mon Sep 02 00:00:00 2025
From: ChatGPT <dev@assistant.ai>
Date: Tue, 02 Sep 2025 14:00:00 -0300
Subject: [PATCH] Hardening crítico: multitenancy, CORS, RBAC, índices únicos,
 rate limit, serviço WhatsApp com auth e paginação

---
 .gitignore                             |  13 ++++
 backend/middlewares.py                 |  51 ++++++++++++++++
 backend/server.py                      | 170 +++++++++++++++++++++++++++++++---
 backend/whatsapp_service.py            |  22 +++++-
 whatsapp-node-service/server.js        |  48 +++++++++++-
 5 files changed, 288 insertions(+), 16 deletions(-)
 create mode 100644 backend/middlewares.py

diff --git a/backend/server.py b/backend/server.py
index 3b2f1ac..9a1e77d 100644
--- a/backend/server.py
+++ b/backend/server.py
@@ -1,22 +1,41 @@
-from fastapi import FastAPI, Depends, HTTPException, status, Request
+from fastapi import FastAPI, Depends, HTTPException, status, Request
 from fastapi.middleware.cors import CORSMiddleware
 from fastapi.routing import APIRouter
 from typing import List
 import os
+from starlette.responses import JSONResponse
 
 from models import User, UserRole, License
 from auth import get_current_user
-from db import db, initialize_system
-from security import check_permission, get_user_permissions
+from db import db, initialize_system
+from security import check_permission, get_user_permissions
+from middlewares import ObservabilityMiddleware, RateLimitMiddleware
+from pydantic import BaseModel
 
 app = FastAPI()
 api_router = APIRouter()
 
-# CORS config
+# CORS config (padronizar X-Tenant-ID em TODAS as camadas)
 app.add_middleware(
     CORSMiddleware,
-    allow_origins=["*"],
+    allow_origins=["*"],
     allow_credentials=True,
-    allow_methods=["*"],
-    allow_headers=["Content-Type", "Authorization", "Accept", "X-Current-Tenant"],
-    expose_headers=["X-Current-Tenant"],
+    allow_methods=["*"],
+    allow_headers=["Content-Type", "Authorization", "Accept", "X-Tenant-ID"],
+    expose_headers=["X-Tenant-ID"],
 )
 
+# Middlewares de observabilidade e rate limit (ativar em produção)
+app.add_middleware(ObservabilityMiddleware)
+app.add_middleware(RateLimitMiddleware)
+
+# ---------- Helpers de Tenant ----------
+def require_tenant(request: Request) -> str:
+    # Header padronizado
+    tenant_id = request.headers.get("X-Tenant-ID")
+    if not tenant_id:
+        # Em produção, NÃO faça fallback silencioso
+        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="X-Tenant-ID ausente")
+    return tenant_id
+
+def add_tenant_filter(base: dict, tenant_id: str) -> dict:
+    f = dict(base or {})
+    f["tenant_id"] = tenant_id
+    return f
+
 # ---------- Rotas ----------
 
 @api_router.get("/health")
@@ -29,67 +48,164 @@ async def health():
     return {"status": "ok"}
 
-@api_router.get("/licenses", response_model=List[License])
-async def get_licenses(current_user: User = Depends(get_current_user)):
-    try:
-        if current_user.role == UserRole.ADMIN or current_user.role == UserRole.SUPER_ADMIN:
-            query_filter = add_tenant_filter({})
-        else:
-            query_filter = add_tenant_filter({"assigned_user_id": current_user.id})
-        items = await db.licenses.find(query_filter).to_list(1000)
-        return items
-    except Exception as e:
-        raise HTTPException(status_code=500, detail=str(e))
+class PageParams(BaseModel):
+    page: int = 1
+    size: int = 50
+
+@api_router.get("/licenses", response_model=List[License])
+async def get_licenses(
+    request: Request,
+    current_user: User = Depends(get_current_user),
+    tenant_id: str = Depends(require_tenant),
+):
+    try:
+        # paginação via query string ?page=&size= (limites razoáveis)
+        try:
+            page = int(request.query_params.get("page", "1"))
+            size = int(request.query_params.get("size", "50"))
+        except ValueError:
+            page, size = 1, 50
+        page = max(1, page)
+        size = min(200, max(1, size))
+
+        if current_user.role == UserRole.SUPER_ADMIN:
+            # Super admin pode ver todos os tenants (atenção: só use se isso fizer sentido no seu domínio)
+            query_filter = {}
+        elif current_user.role == UserRole.ADMIN:
+            query_filter = add_tenant_filter({}, tenant_id)
+        else:
+            query_filter = add_tenant_filter({"assigned_user_id": current_user.id}, tenant_id)
+
+        cursor = db.licenses.find(query_filter).skip((page - 1) * size).limit(size)
+        items = await cursor.to_list(length=size)
+        return items
+    except Exception as e:
+        raise HTTPException(status_code=500, detail=str(e))
 
-def require_permission(permission: str):
+def require_permission(permission: str):
     async def permission_checker(current_user: User = Depends(get_current_user)):
-        user_permissions = await get_user_permissions(current_user.email)
+        # RBAC deve considerar o tenant do usuário
+        user_permissions = await get_user_permissions(current_user.email, current_user.tenant_id)
         if not check_permission(user_permissions, permission):
             raise HTTPException(status_code=status.HTTP_403_FORBIDDEN,
                                 detail=f"Permission required: {permission}")
         return current_user
     return permission_checker
 
-@api_router.get("/stats")
+@api_router.get("/stats")
 async def stats(current_user: User = Depends(require_permission("stats:read"))):
     # ...
     return {"ok": True}
 
-@api_router.get("/demo-credentials")
-async def demo_credentials():
-    return {"email": "demo@demo.com", "password": "demo123"}
+@api_router.get("/demo-credentials")
+async def demo_credentials():
+    # NÃO exponha em produção
+    if os.getenv("ENVIRONMENT", "development") == "production":
+        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)
+    return {"email": "demo@demo.com", "password": "demo123"}
 
 app.include_router(api_router)
 
-@app.on_event("startup")
-async def startup_event():
-    await initialize_system()
+@app.on_event("startup")
+async def startup_event():
+    await initialize_system()
+    await ensure_indexes()
 
+async def ensure_indexes():
+    # Índices únicos por tenant (evitam duplicatas e aceleram consultas)
+    try:
+        await db.users.create_index(
+            [("tenant_id", 1), ("email", 1)],
+            unique=True,
+            name="uniq_tenant_email",
+        )
+        await db.clientes_pf.create_index(
+            [("tenant_id", 1), ("cpf", 1)],
+            unique=True,
+            name="uniq_tenant_cpf",
+        )
+        await db.clientes_pj.create_index(
+            [("tenant_id", 1), ("cnpj_normalizado", 1)],
+            unique=True,
+            name="uniq_tenant_cnpj",
+        )
+        await db.licenses.create_index(
+            [("tenant_id", 1), ("serial", 1)],
+            unique=True,
+            name="uniq_tenant_serial",
+        )
+    except Exception as e:
+        # Logue o erro de índice, mas não derrube a app
+        print(f"[indexes] erro ao criar índices: {e}")
+
 if __name__ == "__main__":
     import uvicorn
     uvicorn.run(app, host="0.0.0.0", port=int(os.getenv("PORT", "8000")))
diff --git a/backend/whatsapp_service.py b/backend/whatsapp_service.py
index 8ad9b92..5f1a2f1 100644
--- a/backend/whatsapp_service.py
+++ b/backend/whatsapp_service.py
@@ -1,21 +1,30 @@
 import os
 import httpx
 
-WHATSAPP_SERVICE_URL = os.getenv("WHATSAPP_SERVICE_URL", "http://localhost:4000")
+WHATSAPP_SERVICE_URL = os.getenv("WHATSAPP_SERVICE_URL", "http://localhost:4000")
+INTERNAL_API_KEY = os.getenv("INTERNAL_API_KEY")
+WHATSAPP_REQUEST_TIMEOUT = float(os.getenv("WHATSAPP_REQUEST_TIMEOUT", "10.0"))
 
 async def send_whatsapp_message(payload: dict) -> dict:
-    async with httpx.AsyncClient() as client:
-        r = await client.post(f"{WHATSAPP_SERVICE_URL}/send", json=payload, timeout=10.0)
+    headers = {}
+    if not INTERNAL_API_KEY:
+        # Em produção, exija a key
+        raise RuntimeError("INTERNAL_API_KEY não configurada")
+    headers["X-Internal-Token"] = INTERNAL_API_KEY
+
+    async with httpx.AsyncClient(timeout=WHATSAPP_REQUEST_TIMEOUT, headers=headers) as client:
+        r = await client.post(f"{WHATSAPP_SERVICE_URL}/send", json=payload)
         r.raise_for_status()
         return r.json()
 
 async def send_whatsapp_bulk(payloads: list[dict]) -> dict:
-    async with httpx.AsyncClient() as client:
-        r = await client.post(f"{WHATSAPP_SERVICE_URL}/bulk", json={"items": payloads}, timeout=20.0)
+    headers = {}
+    if not INTERNAL_API_KEY:
+        raise RuntimeError("INTERNAL_API_KEY não configurada")
+    headers["X-Internal-Token"] = INTERNAL_API_KEY
+    async with httpx.AsyncClient(timeout=WHATSAPP_REQUEST_TIMEOUT, headers=headers) as client:
+        r = await client.post(f"{WHATSAPP_SERVICE_URL}/bulk", json={"items": payloads})
         r.raise_for_status()
         return r.json()
diff --git a/whatsapp-node-service/server.js b/whatsapp-node-service/server.js
index 4a3c2f1..7c5a1e9 100644
--- a/whatsapp-node-service/server.js
+++ b/whatsapp-node-service/server.js
@@ -1,18 +1,48 @@
-const express = require('express');
-const app = express();
+const express = require('express');
+const helmet = require('helmet');
+const rateLimit = require('express-rate-limit');
+const app = express();
 
 app.use(express.json());
+app.use(helmet());
+app.use(rateLimit({
+  windowMs: 60 * 1000,
+  max: 60,
+  standardHeaders: true,
+  legacyHeaders: false,
+}));
 
-// ROTAS EXISTENTES
+const INTERNAL_API_KEY = process.env.INTERNAL_API_KEY;
+
+function requireInternalAuth(req, res, next) {
+  const key = req.header('X-Internal-Token');
+  if (!INTERNAL_API_KEY || key !== INTERNAL_API_KEY) {
+    return res.status(401).json({ error: 'Unauthorized' });
+  }
+  return next();
+}
+
+// ROTAS EXISTENTES (proteger as que disparam envios)
 
-app.post('/send', async (req, res) => {
+app.post('/send', requireInternalAuth, async (req, res) => {
   try {
     const payload = req.body;
     // ... lógica de envio
     return res.json({ ok: true });
   } catch (e) {
     return res.status(500).json({ error: e.message });
   }
 });
 
-app.post('/bulk', async (req, res) => {
+app.post('/bulk', requireInternalAuth, async (req, res) => {
   try {
     const items = req.body.items || [];
     // ... lógica de envio em lote
     return res.json({ ok: true, count: items.length });
   } catch (e) {
     return res.status(500).json({ error: e.message });
   }
 });
+
+app.get('/health', (_req, res) => res.json({ status: 'ok' }));
+
+const port = process.env.PORT || 4000;
+app.listen(port, () => console.log(`[whatsapp-node-service] listening on ${port}`));
diff --git a/backend/middlewares.py b/backend/middlewares.py
new file mode 100644
index 0000000..b11cdef
--- /dev/null
+++ b/backend/middlewares.py
@@ -0,0 +1,51 @@
+from starlette.middleware.base import BaseHTTPMiddleware
+from starlette.requests import Request
+from starlette.responses import Response
+import time
+
+class ObservabilityMiddleware(BaseHTTPMiddleware):
+    async def dispatch(self, request: Request, call_next):
+        start = time.time()
+        try:
+            response: Response = await call_next(request)
+        finally:
+            dur = (time.time() - start) * 1000.0
+            # substitua por logger estruturado
+            path = request.url.path
+            method = request.method
+            status_code = getattr(response, "status_code", 0)
+            print(f"[obs] {method} {path} -> {status_code} ({dur:.1f}ms)")
+        return response
+
+class RateLimitMiddleware(BaseHTTPMiddleware):
+    # Implementação simples de rate limit por IP (coloque Redis em produção)
+    _buckets = {}
+    WINDOW_MS = 60_000
+    LIMIT = 300
+    async def dispatch(self, request: Request, call_next):
+        ip = request.client.host if request.client else "unknown"
+        now = int(time.time() * 1000)
+        bucket = self._buckets.get(ip, [])
+        # limpa janelas antigas
+        bucket = [t for t in bucket if now - t < self.WINDOW_MS]
+        if len(bucket) >= self.LIMIT:
+            from starlette.responses import JSONResponse
+            return JSONResponse({"detail": "Too Many Requests"}, status_code=429)
+        bucket.append(now)
+        self._buckets[ip] = bucket
+        return await call_next(request)
diff --git a/.gitignore b/.gitignore
index 1a2b3c4..5d6e7f8 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,12 @@
 __pycache__/
 node_modules/
 dist/
+.env
+.env.*
+backend/.env
+backend/.env.*
+whatsapp-node-service/.env
+whatsapp-node-service/.env.*
+frontend/.env
+frontend/.env.*
+*.pem
-- 
2.45.0
